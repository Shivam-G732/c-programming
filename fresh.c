#include <stdio.h>
#include <conio.h>

void main(){

    int x, y, z;
    printf("Enter a two number");
    scanf("%d %d", &x, &y);
    z = x + y;
    printf("Addition is %d", z);
    getch();
}